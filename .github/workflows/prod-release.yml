name: Production Release
on:
  workflow_dispatch:
    inputs:
      commit_sha:
        description: 'Commit SHA to checkout (optional)'
        required: false
        default: ''
jobs:
  common-release:
    uses: portal/gitops/.github/workflows/common-release.yml@main
    with:
      image-name: 'portal'
      kustomization-folder: 'webapps'
    secrets: inherit