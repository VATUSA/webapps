name: CI to Staging
on:
  push:
    branches: [master]
jobs:
  common-ci:
    uses: vatusa/gitops/.github/workflows/common-ci.yml@main
    with:
      image-name: 'portal'
      relative-path: 'apps/vatusa/'
    secrets: inherit

